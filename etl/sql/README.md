# SQL Helpers

This folder is reserved for SQL helpers that support the ETL pipeline.

Examples:

- data cleanup queries
- backfill validation queries
- reporting or sanity-check queries for imported data
